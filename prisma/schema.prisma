generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum CandidateStatus {
  APPLIED
  SCREENING
  INTERVIEW
  TECHNICAL
  HR
  OFFER
  HIRED
  REJECTED
}